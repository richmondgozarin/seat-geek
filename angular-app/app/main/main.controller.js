(function(angular) {
  'use strict';

  angular
    .module('app.controllers')
    .controller('Main', mainCtrl);

  mainCtrl.$inject = [
    '$scope',
  ];

  function mainCtrl($scope) {
    var main = this;

  }
})(window.angular);
